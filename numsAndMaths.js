"use strict";

const score = 400;
const balance = new Number("40"); 
//it automatically gets converted into number when something can't be converted into int then it will give NaN
console.log(score);
console.log(balance);